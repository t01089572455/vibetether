name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [20, 24]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Run Stage 0 gates
        run: npm run check
      - run: npm run test:coverage
      - run: npm run audit:stage0
      - run: npm run test:stage0-evidence
      - run: npm run test:compat:v063-live
        env:
          VIBETETHER_REQUIRE_LIVE_V063: '1'
          VIBETETHER_LIVE_V063_ARTIFACT_DIR: ${{ runner.temp }}/vibetether-live-v063
      - name: Preserve live v0.6.3 failure evidence
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: live-v063-${{ matrix.os }}-node${{ matrix.node }}
          path: ${{ runner.temp }}/vibetether-live-v063
          if-no-files-found: ignore
      - run: npm run test:stage0-package
      - name: Record exact package digest
        run: npm run record:stage0-package-digest
      - run: npm pack --dry-run
      - run: npm audit --audit-level=low
