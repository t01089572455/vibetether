---
name: vibetether-built-in-implementation
description: Make the smallest reversible change that satisfies the approved slice.
---

# vibetether-built-in-implementation

Make the smallest reversible change that satisfies the approved slice.

For user-visible UI work, implement one state from the approved golden screen, then render and compare it. Do not propagate other states until the controlled route has current golden, functional, and visual acceptance receipts and locks the design contract.

Stay inside the VibeTether scope envelope. Return the capability outputs and fresh exit evidence. Never override confirmed project truth, the current slice, or a user decision gate.
