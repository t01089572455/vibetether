---
name: vibetether-built-in-tdd
description: Observe a failing check, implement the minimum change, then rerun focused checks.
---

# vibetether-built-in-tdd

Observe a failing check, implement the minimum change, then rerun focused checks.

Stay inside the VibeTether scope envelope. Return the capability outputs and fresh exit evidence. Never override confirmed project truth, the current slice, or a user decision gate.
